# Next.js Environment Variables

# API base URL for signup/validate (browser) and fallback. Default: https://api.useafter.ai
NEXT_PUBLIC_API_BASE_URL=https://api.useafter.ai

# Internal API base for /api/session/login proxy (server-to-server). Bypasses Cloudflare.
# REQUIRED in production when frontend is on Vercel and API is behind Cloudflare.
# Use Azure Container App FQDN, e.g. https://<app>.azurecontainerapps.io
AFTERAI_INTERNAL_API_BASE_URL=

# Auth secret for JWT verification in middleware. Must match backend AFTERAI_AUTH_SECRET. Server-only.
AFTERAI_AUTH_SECRET=

# Comma-separated emails allowed to see the real console at /console. Case-insensitive. Missing/empty = deny all.
# APPROVED_EMAILS=me@domain.com,claudia@domain.com
APPROVED_EMAILS=

# Legacy (optional). Prefer NEXT_PUBLIC_API_BASE_URL.
# NEXT_PUBLIC_API_URL=http://localhost:8000

# Contact form (Enterprise "Talk to us") – sends via Resend.
# REQUIRED for /api/contact in production: add RESEND_API_KEY in Vercel → Settings → Environment Variables.
# Get key: https://resend.com/api-keys
RESEND_API_KEY=
# Recipient. Default: hello@useafter.ai
# CONTACT_TO_EMAIL=hello@useafter.ai
# Optional: "from" address (e.g. contact@useafter.ai after verifying domain in Resend). Default: onboarding@resend.dev
# CONTACT_FROM=AfterAI Contact <contact@useafter.ai>
# CONTACT_FROM_EMAIL=AfterAI Contact <contact@useafter.ai>

# Next.js Environment Variables

# API base URL for signup/validate (browser) and fallback. Default: https://api.useafter.ai
NEXT_PUBLIC_API_BASE_URL=https://api.useafter.ai

# Internal API base for /api/session/login proxy (server-to-server). Bypasses Cloudflare.
# REQUIRED in production when frontend is on Vercel and API is behind Cloudflare.
# Use Azure Container App FQDN, e.g. https://<app>.azurecontainerapps.io
AFTERAI_INTERNAL_API_BASE_URL=

# Auth secret for JWT verification in middleware. Must match backend AFTERAI_AUTH_SECRET. Server-only.
AFTERAI_AUTH_SECRET=

# Legacy (optional). Prefer NEXT_PUBLIC_API_BASE_URL.
# NEXT_PUBLIC_API_URL=http://localhost:8000
